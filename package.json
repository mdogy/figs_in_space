{
  "name": "retro-vector-asteroids",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "find src tests -name '*.js' -type f -delete && echo 'Cleaned stale .js files'",
    "dev": "npm run clean && vite",
    "build": "npm run clean && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "vitest": "^1.2.0"
  }
}
